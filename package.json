{
  "name": "cv-screener",
  "version": "1.0.0",
  "description": "AI-powered CV screening application with dual mode (Local/Cloud)",
  "scripts": {
    "dev": "concurrently -k -s first -n \"API,WEB\" -c \"cyan,magenta\" \"npm run api\" \"npm run web\"",
    "api": "python start_api.py",
    "web": "python start_web.py",
    "install:all": "npm install && cd frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "remark-gfm": "^4.0.1"
  }
}
